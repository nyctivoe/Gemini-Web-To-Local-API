# Gemini cookies — get from Chrome DevTools > Application > Cookies > .google.com
GEMINI_PSID=
GEMINI_PSIDTS=

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=6969

# Proxy (leave empty if not using one)
PROXY_URL=
