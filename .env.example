# Gemini cookies — get from Chrome DevTools > Application > Cookies > .google.com
GEMINI_PSID=
GEMINI_PSIDTS=

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=6969

# API key auth (leave empty to allow all requests)
API_KEY=

# Proxy (leave empty if not using one)
PROXY_URL=
